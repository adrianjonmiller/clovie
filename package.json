{
  "name": "atx-monorepo",
  "private": true,
  "version": "0.0.27",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces",
    "test": "npm run test --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^1.0.4",
    "prettier": "^3.1.1",
    "eslint": "^8.56.0"
  }
}
